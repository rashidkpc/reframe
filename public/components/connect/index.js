import React from 'react';
export default React.createClass({
  
  render() {
    return (
      <div>
        Select one of the connectors below to load data into a the frame
      </div>
    );
  }

});
